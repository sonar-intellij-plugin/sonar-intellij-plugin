language: java
sudo: false
install: true
jdk:
- openjdk8
script:
  - ./gradlew clean buildPlugin
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.sonar/cache'
    - '$HOME/.gradle'
    - '.gradle'
